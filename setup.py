# from dev

from setuptools import find_packages, setup

setup(
    name='Jasmine',
    version=0.1,
    author='jasmine',
    author_email="jasminec4894@gmail.com",
    packages=find_packages(), # goes in folder and finds all packages
install_requires = []
)